# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
# This is required to provide enough memory for the Minecraft decompilation process.
org.gradle.jvmargs=-Xmx4G
org.gradle.daemon=false

group = net.silentchaos512
archivesBaseName = TreasureBags
mod_id = treasurebags

mc_version = 1.14.2
sl_version = 4.+
#jei_version=4.14.4.259

forge_version = 26.0.35
mcp_channel = snapshot
mcp_mappings = 20190620

cf_project_id = 318299
cf_release_type = beta
cf_changelog_filename = CHANGELOG.md
